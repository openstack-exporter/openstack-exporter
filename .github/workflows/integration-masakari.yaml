name: integration-masakari
on:
  pull_request:
  workflow_dispatch:

jobs:
  integration-masakari:
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: "master"
            openstack_version: "master"
            ubuntu_version: "24.04"
    runs-on: ubuntu-${{ matrix.ubuntu_version }}
    name: Deploy OpenStack ${{ matrix.name }} with Masakari and run Masakari integration tests
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
      - name: Deploy devstack
        uses: gophercloud/devstack-action@60ca1042045c0c9e3e001c64575d381654ffcba1
        with:
          branch: ${{ matrix.openstack_version }}
          conf_overrides: |
            enable_plugin masakari https://opendev.org/openstack/masakari ${{ matrix.openstack_version }}
          enabled_services: "openstack-cli-server"
      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version-file: 'go.mod'
      - name: Display clouds.yaml configuration
        run: |
          echo "Contents of /etc/openstack/clouds.yaml:"
          cat /etc/openstack/clouds.yaml || echo "File /etc/openstack/clouds.yaml not found"
      - name: Run OpenStack Integration Tests
        run: bash ./script/integration-suites/masakari
        env:
          DEVSTACK_PATH: ${{ github.workspace }}/devstack
          PACKAGE: "./integration"
          OS_BRANCH: ${{ matrix.openstack_version }}
      - name: Generate logs on failure
        run: ./script/collectlogs
        if: failure()
      - name: Upload logs artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: integration-masakari-${{ matrix.name }}-${{ github.run_id }}
          path: /tmp/devstack-logs/*
